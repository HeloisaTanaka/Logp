#include <stdio.h>
int main()
{
    char s;
    int a, b;
    printf("Digite o símbolo da operação, caso queira sair do programa, digite 'S': ");
    scanf("%c", &s);
    while(s="+" || s="-" || s="*" || s="/" || s="s" || s="S")
}